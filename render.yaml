services:
  - type: web
    name: webmcp-studio
    env: node
    plan: free
    buildCommand: pnpm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
